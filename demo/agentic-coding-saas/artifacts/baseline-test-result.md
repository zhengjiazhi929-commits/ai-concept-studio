# Baseline 测试记录

- 日期：2026-08-03
- 命令：`node --test tests/*.test.mjs`
- 结果：5 项测试，1 项通过，4 项失败
- 状态：符合预期；这是 Agent 开始任务前的真实基线，不是最终产品结果

## 通过

- 客户列表使用本地虚构数据。

## 失败

1. 普通用户不能创建导出任务：实际 `200`，期望 `403`；
2. 管理员创建后台任务：实际 `200`，期望 `202`；
3. 导出任务完成后提供脱敏 CSV：尚无后台任务接口；
4. 普通用户不能查询或下载管理员导出：尚无任务级权限接口。

这组失败将用于录制“读取反馈 → 定位问题 → 修改 → 重新验证”的真实过程。

